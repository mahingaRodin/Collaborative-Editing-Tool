# Server
server.port=7777
server.servlet.context-path=/cet

#security
app.jwt.secret=superSecretKey1234567890!@#Secure
app.jwt.expiration-in-ms=3600000

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/collab_db
spring.datasource.username=postgres
spring.datasource.password=12345
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA & Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Redis Configuration
spring.data.redis.host=localhost
spring.data.redis.port=6379

# Enable Redis-backed Caching
spring.cache.type=redis

# Enable Redis Session Storage
spring.session.store-type=redis

# Logging
logging.level.root=INFO
logging.level.org.springframework.web=DEBUG
